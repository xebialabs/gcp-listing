apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: "$name"
  namespace: "$namespace"
  annotations:
    marketplace.cloud.google.com/deploy-info: '{"partner_id": "digitalai-public", "product_id": "xl-release", "partner_name": "Digital.ai"}'
  labels:
    app.kubernetes.io/name: "$name"
spec:
  descriptor:
    type: xl-release
    version: '24.2'
  selector:
    matchLabels:
      app.kubernetes.io/name: "$name"
  componentKinds:
  # The group is determined from the apiVersion: $GROUP_NAME/$VERSION
  - group: apps
    kind: Deployment
  - group: ''
    kind: Service
